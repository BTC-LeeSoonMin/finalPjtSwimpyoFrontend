import React, { useState } from 'react';
import { Container, Grid } from '@mui/material';

function Main() {

  return (
    <Container component="main" maxWidth="xs" sx={{ marginBottom: '1rem', marginTop: '1rem' }}>
      이미지 슬라이드
    </Container>
  );
}

export default Main;
